#include "main.h"
#include <stdlib.h>

/**
 * _abs - gives the absolute value of an integer.
 * @n: parameter of integer to get the absolute
 * Return: 0 if success
 */

int _abs(int n)
{
	return (abs(n));
}
